import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 10,
    paddingLeft: 5,
    paddingRight: 30,
    marginBottom: 10,
    flexDirection: 'row',
  },
});
